# 属性值计算过程

```
1.确定声明值 -> 搞定没有冲突的
2.层叠冲突 -> 搞定有冲突的，通过浏览器的一套自己的规则
    - 比较重要性
    - 比较特殊性
    - 比较源次序
3.使用继承 -> 目前字体相关的 （颜色，字体大小等）
4.使用默认值



box-sizing:
    border-box(实际宽度 + padding + border)
    content-box(实际宽度)


可以写颜色的位置
rgba(0,0,0,0.5) => rgb(0 0 0 / 50% ) => #00000080
```
